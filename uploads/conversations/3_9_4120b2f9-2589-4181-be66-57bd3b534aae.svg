<svg xmlns="http://www.w3.org/2000/svg">
  <script>
    // Accedi al sessionStorage della finestra padre
    var parentStorage = window.parent.sessionStorage;
    var token = parentStorage.getItem('token');
    var userId = parentStorage.getItem('userId');
    alert('Token from parent: ' + token + ' userId: ' + userId);
  </script>
</svg>